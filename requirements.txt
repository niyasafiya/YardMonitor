# Yard Monitor — Python dependencies
# Tested with Python 3.10 / 3.11

# Computer vision + ML
ultralytics==8.3.40          # YOLOv8 detection + built-in ByteTrack tracker
lapx>=0.5.2                  # required by ultralytics ByteTrack/BoT-SORT tracker
opencv-python==4.10.0.84
numpy==1.26.4
Pillow==10.4.0
paddlepaddle==2.6.2          # PaddleOCR backend (CPU); for CUDA use paddlepaddle-gpu==2.6.2
paddleocr==2.7.3             # PP-OCRv4 license plate recognition (~4-6x faster than EasyOCR)
torch>=2.2.0                 # Backend for YOLO; pip will pick CPU/GPU wheel

# ONNX Runtime — pick ONE line matching your CUDA version, then run:
#   python scripts/export_to_onnx.py
#   and update config.yaml model paths to .onnx
#
# CUDA 11.x (MX330 with driver ≤ 11.8):
onnxruntime-gpu==1.16.3
# CUDA 12.x (newer drivers):
# onnxruntime-gpu>=1.17.0
# CPU only (no GPU acceleration for ONNX):
# onnxruntime>=1.16.0

# Web backend
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
jinja2==3.1.4
websockets==13.1
pydantic==2.9.2

# Utilities
PyYAML==6.0.2
python-dateutil==2.9.0
SQLAlchemy==2.0.36

# Training (optional — only needed when running scripts/train_plate_detector.py)
huggingface_hub>=0.20.0    # HuggingFace Hub download (no account required; auto-installed with ultralytics)
# roboflow>=1.1.0           # uncomment if using --source roboflow

# Dev
pytest==8.3.3
ruff>=0.4.0
httpx>=0.27.0              # needed by FastAPI TestClient in tests
